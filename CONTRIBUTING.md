# Contributing

1. Install dependencies with `npm install`.
2. Run `npm run check` and `npm test`.
3. Run `npm pack --dry-run` before publishing changes.

Keep the library runtime-agnostic. Project-specific settings, paths, and opt-out policy belong in the caller adapter.
